@model GameGroupInfo

@{
    ViewData["Title"] = "Edit Group";
    var games = ViewBag.Games as List<webapp_miniproject.Models.GameInfo> ?? new();
}

<div class="container py-3">
    <div class="row justify-content-center">
        <div class="col-12 col-md-6">

            <div class="d-flex align-items-center gap-3 mb-4">
                <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-sm btn-secondary">← Back</a>
                <h2 class="text-light mb-0">Edit Group</h2>
            </div>

            <div class="card general-block">
                <div class="card-body p-4">
                    <form asp-action="Edit" asp-route-id="@Model.Id" method="post">

                        <div class="mb-3">
                            <label class="form-label text-light">Game</label>
                            <select name="GameId" class="form-select" required>
                                <option value="">— select a game —</option>
                                @foreach (var game in games)
                                {
                                    <option value="@game.Id" selected="@(game.Id == Model.GameId)">
                                        @game.Name
                                    </option>
                                }
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label text-light">Title</label>
                            <input name="Title" type="text" class="form-control"
                                   value="@Model.Title" required />
                        </div>

                        <div class="mb-3">
                            <label class="form-label text-light">Description</label>
                            <textarea name="Description" class="form-control"
                                      rows="4">@Model.Description</textarea>
                        </div>

                        <div class="mb-4">
                            <label class="form-label text-light">
                                Image URL <span class="text-muted">(optional)</span>
                            </label>
                            <input name="ImageUrl" type="url" class="form-control"
                                   value="@Model.ImageUrl" placeholder="https://..." />
                        </div>

                        <button type="submit" class="btn btn-primary w-100">Save Changes</button>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>
